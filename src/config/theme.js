import React from 'react';
import { createMuiTheme } from '@material-ui/core';

const theme = createMuiTheme({
  palette: {
    primary: { main: '#ff6c3f' },
    secondary: { main: '#6fa491' },
  }
});

export default theme;
