import React from 'react';
import { createMuiTheme } from '@material-ui/core';

const theme = createMuiTheme({
  palette: {
    primary: { main: '#ff6c3f' },
    secondary: { main: '#3fd2ff' },
  }
});

export default theme;
